<!-- <% if current_user.follows?(user) %>
  <%= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->
<% if current_user.follows?(user) %>
  <% follow = "followed" %>
  <% else %>
  <% follow = "unfollowed" %>
<% end %>
<button type="submit" name="button" data-user-id="<%= user.id %>"
      data-initial-follow-state="<%= follow %>"
      class="follow-toggle"></button>
